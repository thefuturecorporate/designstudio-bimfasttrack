import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Executive Diploma in BIM Course — 6-8 Month BIM Training | Design Studio",
  description:
    "Complete BIM workflow training covering 15+ software tools, 7 real projects, and 12 modules. From concept design to project handover.",
};

export default function CourseLayout({ children }: { children: React.ReactNode }) {
  return children;
}
