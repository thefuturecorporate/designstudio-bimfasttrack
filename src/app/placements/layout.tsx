import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "100% Placement Record — BIM Training Placements | Design Studio Institute",
  description:
    "Our graduates work at leading construction firms and BIM organizations. 100% placement record with dedicated career support.",
};

export default function PlacementsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
