import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "20+ High-Salary BIM Career Roles — BIM FastTrack | Design Studio",
  description:
    "Explore BIM job roles from BIM Modeler to BIM Director. Learn which roles pay ₹4-20 LPA and how to get there with our Executive Diploma in BIM.",
};

export default function CareerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
